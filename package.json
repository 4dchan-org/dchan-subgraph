{
  "name": "4dchan-subgraph",
  "version": "0.2.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ 4dchan-subgraph",
    "create-local": "graph create --node http://localhost:8020/ 4dchan-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ 4dchan-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 4dchan-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.4",
    "@graphprotocol/graph-ts": "0.29.1",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
